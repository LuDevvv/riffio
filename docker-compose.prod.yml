version: "3.8"

services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: riffio
      POSTGRES_PASSWORD: riffio
      POSTGRES_DB: riffio_db
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7
    restart: always

  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://riffio:riffio@postgres:5432/riffio_db
      - REDIS_HOST=redis
    ports:
      - "4000:3000"
    depends_on:
      - postgres
      - redis

  admin:
    build:
      context: ./apps/admin
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    depends_on:
      - api

  client-landing:
    build:
      context: ./apps/client-landing
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    ports:
      - "3001:3000"
    depends_on:
      - api

volumes:
  postgres-data:
